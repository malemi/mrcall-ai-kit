node_modules/
__pycache__/
*.py[cod]
*.lock
.DS_Store
# Orchestrator session memory: written at run time into the installed checkout
opencode/skills/orchestrator/memory.md
.env
.env.*

# Per-session router memory: ephemeral, per-machine, never shared
docs/sessions/
